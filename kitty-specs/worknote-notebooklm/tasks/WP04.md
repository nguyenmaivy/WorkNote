---
task_id: WP04
phase: implement
priority: high
owner: dev
dependencies: [WP03]
estimated_hours: 10
---

# WP04: Add Source Retrieval and Embeddings Pipeline

## Goals
- Add backend logic for text embedding and retrieval.
- Integrate an embeddings flow using existing AI service wrappers.
- Support search or retrieval by notebook context.

## Acceptance Criteria
- `server/services/embedService.ts` or equivalent is added.
- Source objects store embeddings or retrieval metadata.
- A backend endpoint returns nearby source snippets for a query.
